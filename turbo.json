{
  "pipeline": {
    "prisma:generate": {
      "outputs": ["node_modules/.prisma/**", "node_modules/@prisma/client"]
    },
    "build": {
      "dependsOn": ["^prisma:generate"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
