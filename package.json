{
  "name": "consizeai",
  "private": true,
  "workspaces": [
    "packages/*",
    "services/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "dev": "turbo run dev",
    "install:dep": "turbo run install:dep"
  },
  "devDependencies": {
    "turbo": "latest"
  },
  "packageManager": "yarn@4.8.1"
}
